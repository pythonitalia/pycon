#import "../../fragments/slot.graphql"
query ConferenceSchedule($conferenceId: ID!) {
  conferenceSchedule(conferenceId: $conferenceId) {
    id
    days {
      id
      day
      rooms {
        id
        name
      }
      slots {
        ...SlotFragment
      }
    }
  }
}
