query InvitationLetterDocument($id: ID!) {
  invitationLetterDocument(id: $id) {
    id
    dynamicDocument {
      header
      footer
      pages {
        id
        title
        content
      }
    }
  }
}
