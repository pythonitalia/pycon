#import "./schedule-slot.graphql"

query Schedule($code: String!) {
  conference(code: $code) {
    days {
      day
      rooms {
        id
        name
      }
      slots {
        ...ScheduleSlot
      }
    }
  }
}
