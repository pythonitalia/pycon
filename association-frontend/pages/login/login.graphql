mutation Login($input: LoginInput!) {
  login(input: $input) {
    __typename

    ... on LoginSuccess {
      token
    }

    ... on WrongEmailOrPassword {
      message
    }
    ... on LoginValidationError {
      errors {
        email {
          message
          type
        }
        password {
          message
          type
        }
      }
    }
  }
}
