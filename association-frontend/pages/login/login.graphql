mutation Login($input: LoginInput!) {
  login(input: $input) {
    __typename

    ... on LoginSuccess {
      token
    }

    ... on LoginValidationError {
      errors {
        email {
          message
          type
        }
        password {
          message
          type
        }
      }
    }
  }
}
