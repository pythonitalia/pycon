mutation BookWorkshop($id: ID!, $conference: String!) {
  bookScheduleItem(id: $id, conference: $conference) {
    __typename

    ... on ScheduleItemBookingStatus {
      scheduleItem {
        isBooked
        canBook
      }
    }

    ... on BookScheduleItemError {
      message
    }
  }
}
