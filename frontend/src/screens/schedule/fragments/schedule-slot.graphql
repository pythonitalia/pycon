#import "./submission.graphql"

fragment ScheduleSlotFragment on ScheduleSlot {
  id
  hour
  duration

  items {
    id
    title
    type

    duration

    language {
      id
      name
      code
    }

    submission {
      ...SubmissionFragment
    }

    speakers {
      fullName
    }

    rooms {
      id
      name
      type
    }
  }
}
