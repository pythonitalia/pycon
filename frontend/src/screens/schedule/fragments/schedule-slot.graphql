#import "./submission.graphql"


fragment scheduleSlot on ScheduleSlot {
  id
  hour
  duration

  items {
    id
    title
    type

    submission {
      ...SubmissionFragment
    }

    rooms {
      id
      name
      type
    }
  }
}
