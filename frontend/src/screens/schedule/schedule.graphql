query Schedule($code: String!) {
  conference(code: $code) {
    days {
      day
      scheduleConfiguration {
        hour
        duration
        offset
        size
      }
    }
    rooms {
      name
    }
  }
}
