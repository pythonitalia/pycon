#import "./fragments/schedule-slot.graphql"

query Schedule($code: String!) {
  conference(code: $code) {
    days {
      day
      slots {
        ...scheduleSlot
      }
    }
    rooms {
      id
      name
    }
  }
}
