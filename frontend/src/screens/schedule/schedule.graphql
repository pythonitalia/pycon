query Schedule($code: String!) {
  conference(code: $code) {
    days {
      day
      slots {
        hour
        duration
        offset
        size
        items {
          title
          rooms {
            id
            name
          }
        }
      }
    }
    rooms {
      id
      name
    }
  }
}
