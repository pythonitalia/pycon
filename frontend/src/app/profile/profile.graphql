query MyProfile($conference: String!) {
  me {
    id

    tickets(conference: $conference) {
      id
    }

    name
    fullName
    email
    gender
    dateBirth
    country
    openToRecruiting
    openToNewsletter
  }
}
