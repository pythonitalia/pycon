query ParticipantPublicProfile($id: ID!, $conference: String!) {
  participant(id: $id, conference: $conference) {
    id
    fullname
    publicProfile
    bio
    website
    photo
    twitterHandle
    instagramHandle
    linkedinUrl
    facebookUrl
    mastodonHandle
  }
}
