query Footer($code: String!) {
  conference(code: $code) {
    id
    footer: menu(identifier: "footer") {
      links {
        text: title(language: "en")
        link: href(language: "en")
      }
    }
  }
}
