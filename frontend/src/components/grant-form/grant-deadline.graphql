query GrantDeadline($conference: String!) {
  conference(code: $conference) {
    id
    deadline(type: "grants") {
      id
      start
      end
      status
    }
    form(purpose: GRANT) {
      ...DynamicFormData
    }
  }
}
