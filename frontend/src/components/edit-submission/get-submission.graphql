query GetSubmission($id: ID!) {
  submission(id: $id) {
    id
    title
    type {
      id
      name
    }
    abstract
    elevatorPitch
    notes
    canEdit
    speakerLevel
    previousTalkVideo
    topic {
      id
      name
    }
    duration {
      id
      name
    }
    audienceLevel {
      id
      name
    }
    languages {
      id
      code
      name
    }
    tags {
      id
      name
    }
  }
}
