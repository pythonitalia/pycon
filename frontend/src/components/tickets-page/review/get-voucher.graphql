mutation GetVoucher($conference: String!, $code: String!) {
  getConferenceVoucher(code: $code, conference: $conference) {
    id
    code
    value
    redeemed
    maxUsages
    priceMode
    items
    allItems
    variationId
    validUntil
  }
}
