query GetVoucher($conference: String!, $code: String!) {
  voucher(code: $code, conference: $conference) {
    id
    code
    value
    redeemed
    maxUsages
    priceMode
    items
    allItems
    variationId
    validUntil
  }
}
