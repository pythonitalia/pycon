query KeynotesSection($code: String!) {
  conference(code: $code) {
    id
    keynotes {
      id
      title(language: "en")
      slug(language: "en")
      start
      speakers {
        id
        fullName
        participant {
          id
          photo
          twitterHandle
          instagramHandle
          linkedinUrl
          facebookUrl
          mastodonHandle
        }
      }
    }
  }
}
