mutation UpdateTicket(
  $conferenceCode: String!
  $input: UpdateAttendeeTicketInput!
) {
  __typename
  updateAttendeeTicket(conferenceCode: $conferenceCode, input: $input) {
    __typename

    ... on AttendeeTicket {
      id
      name
      email
      item {
        id
        name
        language
        description
        category
        questions {
          id
          answer {
            answer
            options
          }
          name
          options {
            id
            name
          }
        }
      }
    }
    ... on UpdateAttendeeTicketError {
      message
    }
    ... on TicketReassigned {
      id
      email
    }
  }
}
