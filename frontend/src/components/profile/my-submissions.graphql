query MySubmissions($conference: String!) {
  me {
    id
    submissions(conference: $conference) {
      id
      title
      topic {
        id
        name
      }
      audienceLevel {
        id
        name
      }
      type {
        id
        name
      }
    }
  }
}
