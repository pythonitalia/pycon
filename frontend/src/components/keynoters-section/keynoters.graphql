query KeynotesSection($code: String!) {
  conference(code: $code) {
    id
    keynotes {
      id
      keynoteTitle
      slug
      speakers {
        id
        name
        photo
        highlightColor
      }
    }
  }
}
