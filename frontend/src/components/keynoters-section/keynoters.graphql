query KeynotesSection($code: String!) {
  conference(code: $code) {
    id
    keynotes {
      id
      keynoteTitle
      highlightColor
      speakers {
        id
        name
        photo
      }
    }
  }
}
