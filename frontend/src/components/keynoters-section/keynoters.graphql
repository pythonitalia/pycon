query KeynotesSection($code: String!) {
  conference(code: $code) {
    id
    keynotes {
      id
      title
      slug
      speakers {
        id
        name
        photo
        highlightColor
      }
    }
  }
}
