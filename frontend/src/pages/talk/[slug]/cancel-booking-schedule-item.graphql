mutation CancelBookingScheduleItem($id: ID!) {
  cancelBookingScheduleItem(id: $id) {
    __typename

    ... on ScheduleItem {
      id
      userHasSpot
      hasSpacesLeft
      spacesLeft
    }
  }
}
