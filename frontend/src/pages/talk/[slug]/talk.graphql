query Talk($slug: String!, $code: String!) {
  conference(code: $code) {
    id
    talk(slug: $slug) {
      id
      title
      image
      highlightColor
      description
      hasLimitedCapacity
      spacesLeft

      submission {
        id
        abstract
        elevatorPitch
      }

      speakers {
        ... on ScheduleItemUser {
          id
          fullName
        }

        ... on ScheduleItemNamedUser {
          fullName
        }
      }
    }
  }
}
