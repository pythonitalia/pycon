fragment SubmissionFragment on Submission {
  id
  title(language: $language)

  duration {
    id
    duration
  }

  audienceLevel {
    id
    name
  }

  speaker {
    id
    fullname
  }

  type {
    id
    name
  }

  tags {
    id
    name
  }
}
