#import "../../fragments/submission-accordion.graphql"

query Ranking($conference: String!, $topicId: Int!) {
  conference(code: $conference) {
    id

    ranking(topic: $topicId) {
      isPublic
      rankedSubmissions {
        absoluteRank
        topicRank
        absoluteScore
        submission {
          ...submissionAccordion
          speaker {
            id
            fullName
          }
        }
      }
    }
  }
}
