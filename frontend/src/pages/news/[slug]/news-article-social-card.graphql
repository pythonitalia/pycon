query NewsArticleSocialCard(
  $hostname: String!
  $slug: String!
  $language: String!
) {
  newsArticle(hostname: $hostname, slug: $slug, language: $language) {
    id
    title
    authorFullname
  }
}
