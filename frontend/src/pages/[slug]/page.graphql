#import "../../fragments/generic-page.graphql"

query Page($hostname: String!, $slug: String!) {
  cmsPage(hostname: $hostname, slug: $slug, language: "en") {
    ... on GenericPage {
      ...GenericPageData
    }
  }
}
