#import "../../components/users-table/user-table.graphql"

query Users($after: Int!, $to: Int!) {
  users(after: $after, to: $to) {
    pageInfo {
      totalCount
      hasMore
    }

    items {
      ...UserDataForTable
    }
  }
}
