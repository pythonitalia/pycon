#import "../../helpers/field-error.graphql"

mutation Login($input: LoginInput!) {
  login(input: $input) {
    __typename

    ... on LoginSuccess {
      user {
        id
      }
    }

    ... on LoginValidationError {
      errors {
        email {
          ...FieldError
        }
        password {
          ...FieldError
        }
      }
    }
  }
}
